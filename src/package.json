{
  "name": "ng-chat",
  "version": "0.1.0-beta.2",
  "dependencies": {
    "@angular/common": "*",
    "@angular/core": "*",
    "@angular/forms": "*",
    "rxjs": "*" 
  },
  "devDependencies": {
    "gulp": "3.9.1",
    "gulp-inline-ng2-template": "^4.0.0",
    "typescript": "^2.4.2"
  },
  "scripts": {
    "build": "gulp inline-templates && copy tsconfig.json dist && cd dist && tsc && del tsconfig.json"
  },
  "description": "A simple facebook/linkedin lookalike chat plugin for Angular applications.",
  "author": "Rafael Carvalho",
  "license": "MIT"
}
